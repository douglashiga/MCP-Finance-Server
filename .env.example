# Interactive Brokers Credentials
# Copy this file to .env and fill in your values:
#   cp .env.example .env

TWS_USERID=your_ib_username
TWS_PASSWORD=your_ib_password
TRADING_MODE=paper   # 'paper' or 'live'

# Database Configuration
# Default is SQLite. Set DATABASE_URL to use PostgreSQL.
# Example: postgresql://finance:finance_secret@localhost:5432/finance_db
# In Docker, use: postgresql://finance:finance_secret@postgres:5432/finance_db
DATABASE_URL=sqlite:///./dataloader/finance_data.db

# PostgreSQL Credentials (used by docker-compose)
POSTGRES_PASSWORD=finance_secret

# DataLoader Admin API Security
# For local development only, you can disable API key checks:
# DATALOADER_ALLOW_INSECURE=true
# For production, keep insecure mode disabled and set an API key:
# DATALOADER_API_KEY=change-me
DATALOADER_ALLOW_INSECURE=true
DATALOADER_API_KEY=
